@model ArcheProjectWebApp.Models.ResetPasswordModel
@{
    ViewData["Title"] = "ResetPassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>ResetPassword</h1>

<form asp-action="ResetPassword" asp-controller="Account" method="post">
   
       @* <div class="form-group">
            <label asp-for="Email" class="control-label"></label>
            <input asp-for="Email" class="control-label" />
            <span asp-validation-for="Email" class="control-label"></span>
        </div>*@
        <div class="form-group">
            @Html.HiddenFor(x=>x.Code)
        </div>
        <div class="form-group">
            <label asp-for="NewPassword" class="control-label"></label>
            <input asp-for="NewPassword" class="control-label" />
            <span asp-validation-for="NewPassword" class="control-label"></span>
        </div>
        <div class="form-group">
            <label asp-for="ConfirmPassword" class="control-label"></label>
            <input asp-for="ConfirmPassword" class="control-label" />
            <span asp-validation-for="ConfirmPassword" class="control-label"></span>
        </div>
        <div>
            <input type="submit" class="btn btn-success" value="Reset" />
            <a asp-action="Login" asp-controller="Account" class="btn btn-danger">Cancel</a>
            @*<a asp-action="ForgetPassword" asp-controller="Account" class="btn btn-success">Forget Password?</a>*@
            @*<input type="reset" class="btn btn-danger" value="Cancel" />*@
        </div>
    @*</div>*@
</form>